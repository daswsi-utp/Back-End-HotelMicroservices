server:
  port: 9093
spring:
  output:
    ansi:
      enabled: always
  application:
    name: microservice-rooms
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/msvc-rooms
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
eureka:
  instance:
    hostname: localhost
  client:
    #This line is used for testing.
    #enabled: false
    service-url:
      defaultZone: http://localhost:8761/eureka

