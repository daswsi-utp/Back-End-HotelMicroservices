server:
  port: 9091

spring:
  application:
    name: microservice-promotions
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/db_system_hotel
    username: postgres
    password: kenyiaqp10
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  instance:
    hostname: localhost
  client:
    #This line is used for testing.
    #enabled: false
    service-url:
      defaultZone: http://localhost:8761/eureka